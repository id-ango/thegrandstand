@model IEnumerable<GlobalSoft.Models.AptType>

@{
    ViewBag.Title = "Index";
}

<h2>Tipe Ruangan</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipeID })
        </td>
    </tr>
}

</table>
