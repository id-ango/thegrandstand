@model IEnumerable<GlobalSoft.Models.GlTipe>

@{
    ViewBag.Title = "Index";
}

<h2>Tipe Akun</h2>
<style>
    table, th, td {
        border: 1px solid grey;
        border-collapse: collapse;
        padding: 5px;
    }

        table tr:nth-child(odd) {
            background-color: #f1f1f1;
        }

        table tr:nth-child(even) {
            background-color: #ffffff;
        }
</style>
<p>
    @*Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GlTipeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GlTipeName)
        </td>
          <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GlTipeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GlTipeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GlTipeID })
        </td>
    </tr>
}

</table>
